import { ReduceStatPipe } from './reduce-stat.pipe';

describe('ReduceStatPipe', () => {
  it('create an instance', () => {
    const pipe = new ReduceStatPipe();
    expect(pipe).toBeTruthy();
  });
});
