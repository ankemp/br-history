import { AssetRoutePipe } from './asset-route.pipe';

describe('AssetRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
